# TODO: use Enums

type Case @entity {
  id: ID!
  companyName: String!
  caseType: String!
  description: String!
  region: String!
  profession: String!
  gender: String!
  ageRange: String!
  experience: String!
}

type Stat @entity {
  id: ID!
  regionName: String!
  casesByRegion: Int!
  casesByType: [Int!]
  casesByProfession: [Int!]
  casesByGender: [Int!]
  casesByAgeRange: [Int!]
}
